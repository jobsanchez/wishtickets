/** Max featured events for home split, EventGrid first page, and API default — keep aligned for initialData / queryKey consistency. */
export const EVENT_GRID_FEATURED_LIMIT = 48;

/** Upcoming chunk size for EventGrid infinite query, skeleton grids, and `/api/events` — keep aligned. */
export const EVENT_GRID_UPCOMING_PAGE_SIZE = 8;
